'use client'; import { useState } from 'react'; import { Brain, Sparkles, FileText, Tag, CheckCircle } from 'lucide-react'; export default function AIToolbar({ articleTitle, content, onSuggestion }: any) { const [isGenerating, setIsGenerating] = useState(false); const handleAIAction = async (action: string) => { setIsGenerating(true); await new Promise(resolve => setTimeout(resolve, 2000)); setIsGenerating(false); onSuggestion?.(action + ' result'); }; return ( <div className="bg-gradient-to-r from-blue-50 to-purple-50 p-4 rounded-lg border border-blue-200 mb-4" dir="rtl"> <div className="flex items-center justify-between mb-3"> <div className="flex items-center"> <Brain className="w-5 h-5 text-blue-600 ml-2" /> <h3 className="font-semibold text-gray-900">مساعد الذكاء الاصطناعي</h3> </div> <span className="text-xs bg-blue-100 text-blue-800 px-2 py-1 rounded-full">GPT-4</span> </div> <div className="grid grid-cols-2 md:grid-cols-4 gap-2"> {[ { id: 'title', icon: Sparkles, label: 'توليد عنوان', color: 'blue' }, { id: 'summary', icon: FileText, label: 'تلخيص المحتوى', color: 'green' }, { id: 'tags', icon: Tag, label: 'اقتراح وسوم', color: 'purple' }, { id: 'grammar', icon: CheckCircle, label: 'تصحيح لغوي', color: 'orange' } ].map((tool) => ( <button key={tool.id} onClick={() => handleAIAction(tool.id)} disabled={isGenerating} className="flex items-center justify-center p-3 bg-white rounded-lg shadow-sm border hover:shadow-md transition-all disabled:opacity-50" > <tool.icon className="w-4 h-4 ml-2" /> <span className="text-sm font-medium">{tool.label}</span> </button> ))} </div> {isGenerating && ( <div className="mt-3 flex items-center text-blue-600"> <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-blue-600 ml-2"></div> <span className="text-sm">جاري التوليد...</span> </div> )} </div> ); }
